# -- Database Configuration --
DB_HOST=127.0.0.1
DB_NAME=bookpy
DB_USER=bookpy_user
DB_PASS=your_strong_password

# -- Application URL --
# The base URL of your application. Required for generating correct links in emails.
APP_URL=http://your-domain.com

# -- Admin Credentials --
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your_secure_admin_password
ADMIN_EMAIL=your-admin-email@example.com

# -- Email Configuration --
# MAIL_MODE can be 'terminal' (for local dev), 'resend' (for production)
MAIL_MODE=resend

# Used as the "From" address in emails. Must be a verified domain in Resend.
MAIL_FROM="Your App Name <noreply@your-domain.com>"

# Your API key from Resend.com, required if MAIL_MODE is 'resend'
RESEND_API_KEY=re_your_resend_api_key